#include <iostream>
#include <string>
#include <vector>

using namespace std;

struct Acao {
    string nomeCompanhia;
    string areaAtuacao;
    double valorAtual;
    double valorAnterior;
    double variacao;
};

int main() {
    vector<Acao> carteira(3);
    double valorBusca;


    for (int i = 0; i < 3; i++) {
        cout << "Dados da acao " << (i + 1) << ":" << endl;
        cout << "Nome: ";
        getline(cin >> ws, carteira[i].nomeCompanhia);
        cout << "Valor atual: ";
        cin >> carteira[i].valorAtual;
    }


    cout << "\nInforme um valor para filtrar acoes abaixo dele: ";
    cin >> valorBusca;

    cout << "\n--- Acoes com valor abaixo de " << valorBusca << " ---" << endl;
    for (int i = 0; i < 3; i++) {
        if (carteira[i].valorAtual < valorBusca) {
            cout << carteira[i].nomeCompanhia << " - Valor: R$ " << carteira[i].valorAtual << endl;
        }
    }

    return 0;
}
